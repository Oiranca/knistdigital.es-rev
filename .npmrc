# Force pnpm as the only package manager
# Prevent npm from being used (security policy)

# pnpm config
auto-install-peers=true
shamefully-hoist=false

# Disallow npm usage by blocking installation via npm registry
# This enforces pnpm-only workflow
engine-strict=true
